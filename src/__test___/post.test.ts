describe('posts', () => {
  describe('given the user id', () => {
    it('should  return the post of the user', () => {
      expect(true).toBe(true);
    });
  });
  describe('given the user id', () => {
    it('should  return the post of the user', () => {
      expect(true).toBe(true);
    });
  });
  describe('given the user id', () => {
    it('should  return the post of the user', () => {
      expect(true).toBe(true);
    });
  });
});
